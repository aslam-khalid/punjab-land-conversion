# Copy to .env and fill in for service-account GEE auth (dashboard/server use).
# For local dev, you can skip this and just run `earthengine authenticate` once instead.

GEE_SERVICE_ACCOUNT=
GEE_KEY_FILE=
